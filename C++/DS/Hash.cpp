#include<iostream>
#include<set>

using namespace std;

void findPair(int* A) {
	int n=sizeof(A)/sizeof(A[0]);
	unordered_multiset<int> h;
	for(int i=0;i<n;i++) {
		
	}
}

int main(int argc,char* argv[]) {
	int A[]={0, -1, 2, -3, 1};
	findPair(A);
	return 0;
}